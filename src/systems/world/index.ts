/**
 * World System
 *
 * Manages time, weather, and global events.
 * Will be expanded in Chunk 7.
 */
export type {
  WorldState,
  WorldEvent,
  TimeOfDay,
  Weather,
  WorldEventType,
} from './types';
export { emptyWorldState } from './types';
