/**
 * Director System
 *
 * Manages visitor spawning and lifecycle.
 * Will be implemented in Chunk 3.
 */
export type { Visitor, VisitorType, DirectorState } from './types';
export { emptyDirectorState } from './types';
